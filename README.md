# Very simple Docker image example

NOTE: Makefile is for BSD make.  Use `bmake` on Linux systems.

## Build

`make build`

## Run

`make run`

## Push image to Docker Cloud

`make push`

You need define `DOCKER_USER_ID` in Makefile or Makefile.local.
